const initialState = {
  orderNumber: 0,
  orderRequest: false,
  orderFailed: false
}

export const order = (state = initialState, action) => {
  switch (action.type) {

    default: return state
  }
}
