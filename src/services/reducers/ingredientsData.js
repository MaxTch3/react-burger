const initialState = {
  data: [],
  dataRequest: false,
  dataFailed: false,
}

export const ingredientsData = (state = initialState, action) => {
  switch (action.type) {

    default: return state
  }
}


